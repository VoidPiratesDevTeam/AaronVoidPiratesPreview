var assert = require("assert"),
    ControlLoop = require('../shared/controlLoop')


describe('ControlLoop', function(){
  describe('#construct()', function(){
    it('', function(){
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    })
  })
})
